# 프로젝트 개발 가이드

## 목적

이 저장소는 Go 버전 관리자 CLI(`gu`) 프로젝트입니다. 파일 시스템, 네트워크, 외부 명령 실행처럼 부작용이 큰 기능이 있으므로 **테스트를 행동 명세로 먼저 작성하고 구현하는 TDD 방식**을 기본으로 합니다.

## 작업 순서

1. 관련 모듈과 기존 테스트를 먼저 읽습니다.
2. 요구사항과 실패/경계 사례를 Given-When-Then 형태로 정리합니다.
3. 실패하는 테스트를 먼저 추가합니다.
4. 테스트를 통과시키는 최소 구현을 합니다.
5. 리팩터링 후 테스트를 다시 실행합니다.
6. 변경 전후의 부작용과 rollback 가능성을 확인합니다.

## 테스트 원칙

- 모든 새 동작에는 정상 경로와 실패 경로 테스트를 함께 추가합니다.
- 파일 시스템은 `t.TempDir()`를 사용하고 실제 홈 디렉터리나 시스템 경로를 사용하지 않습니다.
- 네트워크는 `httptest.Server` 또는 주입된 HTTP 클라이언트를 사용합니다.
- `go`, `tar`, `sudo`, `pkgutil`, `winget` 같은 외부 명령은 테스트에서 실행하지 않습니다. command runner를 주입하거나 함수 seam을 사용해 호출, 인자, 재시도, 오류를 검증합니다.
- 테스트는 전역 상태와 실행 순서에 의존하지 않아야 합니다. 전역 함수/플래그를 변경한 테스트는 반드시 `t.Cleanup`으로 복구합니다.
- 테스트 간 공유 상태가 없으면 `t.Parallel()`을 고려합니다.
- 오류는 단순히 non-nil인지뿐 아니라 중요한 최종 상태, rollback, 오류 종류/메시지도 검증합니다.
- archive 처리, 심볼릭 링크, 삭제 로직은 path traversal, symlink escape, 권한 오류를 우선적으로 검증합니다.
- 테스트를 약화시키기 위해 구현에 맞춰 기대값을 바꾸지 말고, 사용자에게 보이는 계약을 먼저 결정합니다.

## 실행 명령

변경 후 최소한 다음을 실행합니다.

```bash
gofmt -w <changed-go-files>
go test ./... -count=1
go vet ./...
go test -race ./...
```

커버리지를 확인합니다.

```bash
go test ./... -coverprofile=coverage.out
go tool cover -func=coverage.out
```

현재 목표는 전체 statement coverage 60% 이상이며, 핵심 패키지는 가능한 한 더 높은 수준을 유지하는 것입니다. 커버리지 숫자만 올리기 위한 무의미한 테스트는 추가하지 않습니다.

## 설계 원칙

- 외부 시스템 경계는 인터페이스 또는 함수 주입으로 격리합니다.
- CLI는 `RunE`를 통해 오류를 반환하고 `os.Exit`를 직접 호출하지 않습니다.
- 출력은 `cmd.OutOrStdout()`/`cmd.ErrOrStderr()` 또는 주입된 writer를 사용합니다.
- 설치/삭제는 실패 시 기존 상태를 보존하거나 명확히 rollback합니다.
- 시스템 경로 삭제는 allowlist와 안전성 검사를 우회하지 않습니다.
- 새 기능을 추가할 때 package-level mutable state를 늘리지 않습니다.

## 코드 리뷰 체크리스트

- [ ] 테스트가 먼저 요구사항과 경계 조건을 설명하는가?
- [ ] 정상, 실패, 취소/재시도, 부분 실패 시나리오가 있는가?
- [ ] 테스트가 실제 네트워크/홈 디렉터리/권한/외부 명령에 의존하지 않는가?
- [ ] 기존 설치나 캐시를 손상시키지 않는가?
- [ ] 모든 변경 파일에 대해 gofmt, test, vet, race를 실행했는가?
- [ ] 커버리지 감소와 테스트되지 않은 핵심 경로가 없는가?

## 커밋

커밋 메시지는 변경 목적을 드러내는 형식을 사용합니다.

- `test: ...` 테스트 우선 시나리오 추가
- `refactor: ...` 테스트 동작을 유지하는 구조 개선
- `fix: ...` 버그 수정
- `ci: ...` CI/품질 게이트 변경

실제 테스트를 통과시키지 못한 변경은 완료로 보고하지 않습니다.
